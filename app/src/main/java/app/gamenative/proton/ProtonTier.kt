package app.gamenative.proton

/**
 * Task 5 — ProtonDB compatibility tier from community reports.
 * Used for badge on game cards and in game settings.
 */
enum class ProtonTier {
    PLATINUM,
    GOLD,
    SILVER,
    BRONZE,
    BORKED,
    PENDING,
    UNKNOWN,
}
